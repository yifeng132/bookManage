<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cn.bookmanager.mapper.BookMapper">

    <select id="list" resultType="com.cn.bookmanager.domain.entity.Book">
        select * from book
        <where>
            <if test="bookName != null and bookName != ''">
                and book_name like concat('%', #{bookName}, '%')
            </if>
            <if test="author != null and author != ''">
                and author like concat('%', #{author}, '%')
            </if>
        </where>
        order by id desc
    </select>

    <insert id="insert">
        insert into book(book_name, author, price)
        values(#{bookName}, #{author}, #{price})
    </insert>

    <update id="updateById">
        update book
        set book_name = #{bookName},
            author = #{author},
            price = #{price}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from book where id = #{id}
    </delete>

    <select id="selectById" resultType="com.cn.bookmanager.domain.entity.Book">
        select id, book_name, author, price, create_time
        from book
        where id = #{id}
    </select>

</mapper>